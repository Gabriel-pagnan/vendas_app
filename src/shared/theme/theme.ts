import { grayTheme, netraulTheme, purpleTheme, redTheme, orangeTheme } from './colors';

export const theme = {
    colors: {
        redTheme,
        grayTheme,
        orangeTheme,
        netraulTheme,
        purpleTheme,
    },
};
