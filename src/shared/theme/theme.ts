import { grayTheme, netraulTheme, purpleTheme, redTheme } from './colors';

export const theme = {
    colors: {
        grayTheme,
        netraulTheme,
        purpleTheme,
        redTheme,
    },
};
